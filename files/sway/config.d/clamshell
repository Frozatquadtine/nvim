# Clamshell Mode
# set $laptop <eDP-1>
# bindswitch --reload --locked lid:on output $laptop disable && pkexec brillo -O && swaylock
# bindswitch --reload --locked lid:off output $laptop enable && pkexec brillo -I

exec_always ~/.config/sway/scripts/clamshell.sh
